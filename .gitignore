.DS_Store
node_modules/

# Microsoft-copyrighted inputs: never committed
guest/ddk/
image/*.img
image/*.img.*
image/parts/
image/floppies/
# Josh's own install media, expanded and staged for the build (Best of Microsoft Entertainment
# Pack): the built image carries it, git never does. changes/games/ stays SkiFree-only so a clone
# without this directory still builds a working image.
image/changes-local/
*.img
*.iso
*.7z

# build outputs
build/
guest/**/build/
guest/**/obj/
web/dist/
tools/uasm/

# DDK-derived includes (Microsoft-copyrighted), regenerated by tools
guest/driver/inc/

# throwaway framebuffer captures
shots/
image/boot.state.gz
image/current.json
image/boot-*.state.gz

# Vercel CLI project link and its OIDC token file
.vercel
.env*

# python bytecode (tools/grpadd.py imports ne-icons.py)
__pycache__/
